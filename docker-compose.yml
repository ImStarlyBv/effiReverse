version: '3.8'

services:
  effi-api:
    build: .
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    volumes:
      - ./session:/app/session
    environment:
      - EFFI_EMAIL=l_urbaez@yahoo.com
      - EFFI_PASSWORD=DV2TE.e3kFp5anR
      - COOKIE_FILE=/app/session/effi_session.txt
      - PORT=${PORT:-5000}
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:${PORT:-5000}/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
